{% extends 'base.html.twig' %}

{% block title %}vehicule{% endblock %}

{% block body %}
<a class= "btn btn-success mt-2" href="{{path('data_new')}}">Ajout vehicule</a>
<table class="table">
<div class=""></div>
	<thead>
		<tr>
           <th>Titre</th>
            <th>Marque</th>
            <th>Modele</th>
            <th>Description</th>
            <th>Photo</th>
            <th>Prix_jounalier</th>
            <th>Date_Enregistrement</th> 
            <th>Action</th>
        </tr>
	</thead>
	<tbody>
        {% for vehicule in vehicule %}
         <tr>
             <td>{{vehicule.titre}}</td>
             <td>{{vehicule.marque}}</td>
             <td>{{vehicule.modele}}</td> 
             <td>{{vehicule.description}}</td>
             <td><img src="{{vehicule.photo}}" width="150px"></td>
             <td>{{vehicule.prixJounalier}}€</td>
            <td>{{vehicule.dateEnregistrement|date('d/m/Y ')}}</td> 
            <td>
            <a href="{{path('data_edit',{id: vehicule.id})}}" class="text-dark"><i class="fas fa-edit"></i></a>
            <a href="{{path('data_delete',{id: vehicule.id})}}" class="text-dark"><i class="fas fa-trash"></i></a>
            </td> 
         </tr>
            
        {% endfor %}
    </tbody>

</table>
{% endblock %}
